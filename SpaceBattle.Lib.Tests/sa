//  var mockStartable = new Mock<IMoveCommandStartable>();
//         var mockUObject = new Mock<IUObject>();
//         var mockQueue = new Mock<IQueue>();
//         var initialVelocity=new Vector(new int[]{1, 1});

//         mockStartable.SetupGet(x => x.Queue).Returns(new mockQueue()).Verifiable();
//         mockStartable.SetupGet(x => x.UObject).Returns(mockUObject.UObject).Verifiable();
//         mockStartable.SetupGet(x => x.Parameters).Returns(initialVelocity).Verifiable();

//         var StartMoveCommand = new StartMoveCommand(mockStartable.Object);
//         StartMoveCommand.Execute();

//         Assert.NotNull(mockStartable.Object.Queue.Pop());
//         mockUObject.Verify();
